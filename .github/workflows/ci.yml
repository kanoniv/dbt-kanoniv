name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  compile:
    name: Compile check
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: dbt_test
          POSTGRES_PASSWORD: dbt_test
          POSTGRES_DB: dbt_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dbt
        run: pip install dbt-postgres

      - name: Install packages
        working-directory: integration_tests
        run: dbt deps --profiles-dir .
        env:
          POSTGRES_HOST: localhost
          POSTGRES_USER: dbt_test
          POSTGRES_PASSWORD: dbt_test
          POSTGRES_DB: dbt_test

      - name: dbt compile
        working-directory: integration_tests
        run: dbt compile --profiles-dir . --target postgres
        env:
          POSTGRES_HOST: localhost
          POSTGRES_USER: dbt_test
          POSTGRES_PASSWORD: dbt_test
          POSTGRES_DB: dbt_test

  integration-tests:
    name: Integration tests (Postgres)
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: dbt_test
          POSTGRES_PASSWORD: dbt_test
          POSTGRES_DB: dbt_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dbt
        run: pip install dbt-postgres

      - name: Install packages
        working-directory: integration_tests
        run: dbt deps --profiles-dir .
        env:
          POSTGRES_HOST: localhost
          POSTGRES_USER: dbt_test
          POSTGRES_PASSWORD: dbt_test
          POSTGRES_DB: dbt_test

      - name: Seed test data
        working-directory: integration_tests
        run: dbt seed --profiles-dir . --target postgres
        env:
          POSTGRES_HOST: localhost
          POSTGRES_USER: dbt_test
          POSTGRES_PASSWORD: dbt_test
          POSTGRES_DB: dbt_test

      - name: Run test models
        working-directory: integration_tests
        run: dbt run --profiles-dir . --target postgres
        env:
          POSTGRES_HOST: localhost
          POSTGRES_USER: dbt_test
          POSTGRES_PASSWORD: dbt_test
          POSTGRES_DB: dbt_test

      - name: Run assertions
        working-directory: integration_tests
        run: dbt test --profiles-dir . --target postgres
        env:
          POSTGRES_HOST: localhost
          POSTGRES_USER: dbt_test
          POSTGRES_PASSWORD: dbt_test
          POSTGRES_DB: dbt_test
